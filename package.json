{
  "name": "transaction-system",
  "version": "1.0.0",
  "description": "Motor de Processamento Financeiro e de Estatisticas de Alta Volumetria (PoC)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only -r tsconfig-paths/register src/server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:load": "npx --yes autocannon -c 100 -d 30 -m POST -H \"Content-Type: application/json\" -b '{\"amount\": 15000, \"currency\": \"BRL\"}' http://localhost:3000/transactions"
  },
  "keywords": [],
  "author": "João Vitor Camargo",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "amqplib": "^2.0.1",
    "dotenv": "^17.4.2",
    "fastify": "^5.10.0",
    "mongoose": "^8.5.2",
    "oracledb": "^7.0.1",
    "winston": "^3.13.1"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "@types/oracledb": "7.0.1",
    "@vitest/coverage-v8": "^4.1.10",
    "ts-node-dev": "^2.0.0",
    "tsc-alias": "^1.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
